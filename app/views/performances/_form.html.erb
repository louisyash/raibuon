<%#= simple_form_for @performances, html: { class:"form-control" }, data: { test_target: "input", action: "keyup->test#send" } do |f| %>
  <%#= f.input :query %>
  <%#= f.input :photo, as: :file %>
  <%#= f.submit :submit, class:"btn btn-ghost mt-2" %>
  <%# end %>
  <%= form_tag performances_path, method: :get do %>
    <%= text_field_tag :query,
    params[:query],
    data: {
          test_target: 'input',
          action: 'keyup->test#send'
          },
    placeholder: "Find a show"
    %>
    <%= submit_tag "Search", class: "btn btn-ghost mt-2" %>
  <% end %>
